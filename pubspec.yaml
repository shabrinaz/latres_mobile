name: latihan_responsi
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  # State Management (GetX)
  get: ^4.6.6
  # HTTP Request
  dio: ^5.0.0
  # Database Lokal (Hive)
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  # Status Login
  shared_preferences: ^2.2.2 
  # Notifikasi Lokal
  flutter_local_notifications: ^17.0.0
  # Utility
  cached_network_image: ^3.3.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.8

flutter:
  uses-material-design: true
